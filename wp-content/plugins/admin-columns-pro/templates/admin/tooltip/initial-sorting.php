<template id="doc-initial-sorting">
	<p>
		<?=
		__('Configure the default sorting for this table view by selecting the column used for ordering (orderby) and the sort direction (ascending or descending).', 'codepress-admin-columns') ?>
	</p>

	<p>
        <?=
        __('This default sorting is applied automatically when the table is first loaded.', 'codepress-admin-columns') ?>
	</p>

	<p>
        <?=
        __('If a user manually sorts a column, their selected sorting will override this default for that session.', 'codepress-admin-columns') ?>
	</p>
</template>