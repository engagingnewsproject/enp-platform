{% extends "base.twig" %}

{% block content %}
	<div class="wrapper--pad">
		<div class="container grid grid--gap-30 grid--col-12">
			{% if archive.filters %}
				<div class="archive__sidebar">
					{% include 'filters.twig' with { filters: archive.filters } %}
				</div>
				<div class="archive__content home-section">
					{% if archive.postType.name == 'team' %}
						{% include 'partial/teammates.twig' with { mates: archive.posts } %}
					{% else %}
						<h2>Media Ethics</h2>
						<hr />
						{% include 'partial/ethics-links.twig' %}
					{% endif %}
				</div>
			{% else %}
				{% include 'tiles.twig' with { intro: archive.intro, posts: archive.posts } %}
			{% endif %}
		</div>
	</div>
{% endblock %}
