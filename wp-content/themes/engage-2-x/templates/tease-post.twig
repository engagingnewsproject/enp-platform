{% extends "tease.twig" %}


{% block content %}
	{% if post.thumbnail %}
		<img class="tease__img"
		     src="{{ post.thumbnail.src('thumbnail') }}"
		     srcset="{{ post.thumbnail.srcset }}"
		     sizes="{{ post.thumbnail.img_sizes }}"
		     alt="{{ post.thumbnail.alt }}"
		     loading="eager" />
	{% endif %}
	<div class="tease__text">
		<h3 class="tease__title"><a href="{{ post.link }}">{{ post.title }}</a></h3>
		<p class="tease__date">{{ post.date }}</p>
		<p class="tease__excerpt">{{ post.excerpt.length(40) }}</p>
	</div>
{% endblock %}
