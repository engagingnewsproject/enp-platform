var Flickity = require('flickity');

var flkty = new Flickity( ".carousel-main", {
	// options
	wrapAround: true,
  contain: true,
});

	// draggable: '>1',
	// wrapAround: true,
	// adaptiveHeight: true,
	// imagesLoaded: true,
	// lazyLoad: true,
	// percentPosition: true,
	// contain: true,
